module.exports = {
  showChannels: {
    side: true,
    direct: true,
    vehicle: true,
    group: true,
    admin: true,
    privateadmin: true,
    default: true,
    commands: true,
    joins: true,
    global: true
  },
  channels: {
    side: 'rcon',
    direct: 'rcon',
    vehicle: 'rcon',
    group: 'rcon',
    admin: 'rcon',
    privateadmin: 'rcon',
    default: 'rcon',
    commands: 'rcon',
    joins: 'rcon',
    global: 'rcon'
  }
};
